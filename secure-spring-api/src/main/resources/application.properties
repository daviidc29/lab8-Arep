spring.application.name=secure-spring-api

server.port=${PORT:8443}

app.data-dir=${APP_DATA_DIR:./data}
app.cors-allowed-origin=${ALLOWED_ORIGIN:http://localhost:8080}
app.security.token-ttl-minutes=${TOKEN_TTL_MINUTES:120}

management.endpoints.web.exposure.include=health,info